These are tests done on a dataset for PSs extending below 1-ph, generated using nside=2048.

All the scans include NFW PS template. Other templates are labeled in file names.

All scans use 0.8 deg 3FGL masks, King PSF.
